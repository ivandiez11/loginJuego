<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoginJuego" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LoginJuego" parent="Base.Theme.LoginJuego" />
    <style name="Widget.App.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <!-- Color del "hint" cuando está flotando o enfocado -->
        <item name="hintTextColor">@color/white</item>
        <!-- Color de la línea inferior y el borde cuando está enfocado -->
        <item name="boxStrokeColor">@color/white</item>
        <!-- Color del texto que escribe el usuario -->
        <item name="android:textColorHint">#A0A0A0</item> <!-- Color del hint cuando no está enfocado -->
    </style>

</resources>